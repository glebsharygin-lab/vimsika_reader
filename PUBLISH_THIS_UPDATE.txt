Upload the contents of `UPLOAD_TO_GITHUB_0.20.9` to the root of the GitHub
repository, replacing existing copies and preserving folders exactly as shown.

Do not upload `data/corpus.json`. It is the large readable build artifact and is
not needed by GitHub Pages. Upload the root-level `corpus.js` instead.

Main files included:

1. `.nojekyll`
2. `index.html`
3. `auth-config.js`
4. `app.js`
5. `analysis.js`
6. `styles.css`
7. `corpus.js`
8. `editorial-overrides.json`
9. `README.md`
10. `PUBLISH_THIS_UPDATE.txt`

Updated scripts included:

1. `scripts/build_trial_corpus.py`
2. `scripts/smoke_reader.cjs`
3. `scripts/segment_kalupahana_yuda_batch5.py`
4. Existing helper scripts already listed in earlier upload packages.

Updated source-witness folders included:

1. `source-witnesses/README.md`
2. `source-witnesses/san_kalupahana_1987/`
3. `source-witnesses/eng_kalupahana_1987/`
4. `source-witnesses/jpn_yuda_watakushi_yuishiki/`
5. Existing live source-witness folders from the previous upload package.

The new `index.html` uses asset version `0.20.9` for `app.js`, `analysis.js`,
`styles.css`, and `corpus.js`, plus version `0.9.1` for `auth-config.js`, so
GitHub Pages and visitors' browsers fetch the current interface and Worker
configuration.

After GitHub Pages finishes deploying, open:

https://glebsharygin-lab.github.io/vimsika_reader/?v=0.20.9

Expected result:

- The sidebar displays `Build 0.20.9 · Corpus 0.8.0-trial`.
- The page header reads `Viṃśikā Research Environment`.
- Sources & rights lists 37 witnesses.
- New complete witnesses appear as `Sanskrit · Kalupahana 1987` and
  `English · Kalupahana 1987`.
- The new partial Japanese excerpt witness appears as
  `Japanese · Yuda, “Watakushi no Yuishiki”`.
- Verse 1 and verse 2 each show a clean full-width click-to-load YouTube
  player before the verse card.
- The main shell summary reports 155,206 addressable tokens, 255 generated
  sections, 265 DharmaNexus-based alignment rows, and 1,639 preliminary word
  links.

If publishing from the command line, copy the contents of the generated
`UPLOAD_TO_GITHUB_0.20.9` folder into your local clone, then run:

```bash
git add .
git commit -m "Update reader build 0.20.9"
git push
```

DharmaNexus reuse was authorized in writing on 20 June 2026. This build uses
264 source segments and 416 Tibetan/Chinese match records as 265 local rows.
Direct local anchoring succeeds for 211 Tibetan and 170 Paramārtha Chinese
spans; unresolved records and other witnesses remain clearly marked
provisional projections.
